# Core
streamlit>=1.40.0
langgraph>=0.3.0
langgraph-checkpoint-postgres>=2.0.0
langchain-openai>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# Postgres
psycopg[binary,pool]>=3.2.0
psycopg-pool>=3.2.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
uuid6>=2024.1.0  # better UUIDs if needed, but stdlib is fine

# Optional for better UX / tools
# httpx  # if adding tools that need HTTP
